import React, { useState } from "react";
import { Container, Box } from "@mui/system";

const Live = () => {
  return (
    <section>
      <Container
        maxWidth="xl"
        sx={{
          background: "#F2F5F9",
          borderRadius: "16px",
        }}
      ></Container>
    </section>
  );
};

export default Live;
